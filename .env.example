# Dayby server environment.
# Copy to .env for local overrides. NEVER commit real secrets.
# docker-compose already provides sane defaults, so .env is optional for local dev.

# --- MongoDB ---
MONGODB_URI=mongodb://mongo:27017
DB_NAME=dayby

# --- Providers (mock needs no API keys) ---
LLM_PROVIDER=mock
STT_PROVIDER=mock

# --- Real Gemini LLM (handles Korean and any language) ---
# To enable: set LLM_PROVIDER=gemini and paste a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# --- Auth ---
# Generate a strong random value before any real deployment.
JWT_SECRET=change-me-in-production

# --- App ---
APP_ENV=development
